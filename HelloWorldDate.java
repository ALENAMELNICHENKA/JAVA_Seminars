import java.time.LocalDateTime;

/**
 * HelloWorldDate
 */
public class HelloWorldDate {

    public static void main(String[] args) {

        System.out.println("Hello, World");

        System.out.println(LocalDateTime.now());

        /* 
        Типы данных:

        int num = 3;
        double d = 4.6;
        float d = 4.6; но меньше знаков после запятой
        String = "Hello";
        char c = 't'; - символьный тип данных
        boolen b = true;

       Объявление массивов:
        
        int[] array = new int[6];
        int[] array1 = {5, 5, 8, 3, 2, 1}; если сразу задаём значение

        for (int i = 0; i < array1.length; i++)
            System.out.println(array[i]);

            Вывод данных - System.out.println(array[i]);
            Ввод данных - Scanner scanner = new Scanner(System.in); - открываем внешний поток(из терминала берутся данные)
            scanner.close(); - закрыть поток

    Тернарные операторы:

    ?  - тогда
    : - иначе
       */ 
      
    }
    
}
